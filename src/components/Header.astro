<header>
	<div class="header-inner">
		<a class="logo" href={`${import.meta.env.BASE_URL}/`}>TSY</a>

<nav>
	<a href={`${import.meta.env.BASE_URL}/`}>首页</a>
	<a href={`${import.meta.env.BASE_URL}/posts/`}>文章</a>
	<a href={`${import.meta.env.BASE_URL}/projects/`}>项目</a>
	<a href={`${import.meta.env.BASE_URL}/about/`}>关于</a>
</nav>
	</div>
</header>

<style>
	header {
		border-bottom: 1px solid #e8e8e8;
	}

	.header-inner {
		max-width: 900px;
		margin: 0 auto;
		padding: 28px 24px;
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	.logo {
		font-size: 20px;
		font-weight: 700;
		letter-spacing: 0.08em;
		color: #202020;
		text-decoration: none;
	}

	nav {
		display: flex;
		gap: 28px;
	}

	nav a {
		color: #666;
		text-decoration: none;
		font-size: 14px;
	}

	nav a:hover {
		color: #111;
	}

	@media (max-width: 600px) {
		.header-inner {
			padding: 22px 20px;
		}

		nav {
			gap: 16px;
		}
	}
</style>
